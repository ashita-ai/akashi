# Akashi environment variables.
#
# Copy to .env in the repo root:
#   cp docker/env.example .env
#
# Then start the stack:
#   docker compose up -d
#   docker exec akashi-ollama ollama pull mxbai-embed-large   # first run only

# ──────────────────────────────────────────────────────────────
# Postgres
# ──────────────────────────────────────────────────────────────

# POSTGRES_PASSWORD=akashi

# ──────────────────────────────────────────────────────────────
# Admin API key
# ──────────────────────────────────────────────────────────────

# Bootstrap API key for the initial admin agent. Change this in production.
# AKASHI_ADMIN_API_KEY=changeme

# ──────────────────────────────────────────────────────────────
# Embedding provider
# ──────────────────────────────────────────────────────────────

# Default is "ollama" — uses the Ollama container in the stack.
# Set to "openai" with a valid key, or "noop" to disable embeddings.
# AKASHI_EMBEDDING_PROVIDER=ollama

# OpenAI (alternative to Ollama):
# OPENAI_API_KEY=
# AKASHI_EMBEDDING_MODEL=text-embedding-3-small
# AKASHI_EMBEDDING_DIMENSIONS=1024

# Ollama model (default: mxbai-embed-large):
# OLLAMA_MODEL=mxbai-embed-large

# ──────────────────────────────────────────────────────────────
# Qdrant vector search (optional)
# ──────────────────────────────────────────────────────────────

# To enable local Qdrant, start with --profile qdrant:
#   docker compose --profile qdrant up -d
#
# For Qdrant Cloud, set these and skip the profile:
# QDRANT_URL=https://xyz.cloud.qdrant.io:6334
# QDRANT_API_KEY=
# QDRANT_COLLECTION=akashi_decisions

# ──────────────────────────────────────────────────────────────
# Observability (optional)
# ──────────────────────────────────────────────────────────────

# OTEL_EXPORTER_OTLP_ENDPOINT=
# OTEL_SERVICE_NAME=akashi

# ──────────────────────────────────────────────────────────────
# Logging
# ──────────────────────────────────────────────────────────────

# AKASHI_LOG_LEVEL=debug
